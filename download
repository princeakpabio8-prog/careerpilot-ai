# CareerPilot AI

CareerPilot AI is an approval-first job search assistant for remote and visa-sponsorship roles.

## Phase 1 included

- FastAPI backend
- Next.js frontend
- Supabase-ready database schema
- Health and jobs endpoints
- Dashboard starter
- Environment templates

## Project structure

```text
careerpilot-ai/
├── backend/
├── frontend/
├── database/
└── README.md
```

## 1. Backend setup

```bash
cd backend
python -m venv .venv
```

Activate the virtual environment.

Windows PowerShell:

```powershell
.\.venv\Scripts\Activate.ps1
```

macOS/Linux:

```bash
source .venv/bin/activate
```

Install dependencies:

```bash
pip install -r requirements.txt
```

Copy the environment file:

```bash
cp .env.example .env
```

Run the backend:

```bash
uvicorn app.main:app --reload
```

Backend URL:

```text
http://localhost:8000
```

API docs:

```text
http://localhost:8000/docs
```

## 2. Frontend setup

```bash
cd frontend
npm install
cp .env.local.example .env.local
npm run dev
```

Frontend URL:

```text
http://localhost:3000
```

## 3. Database

Open Supabase SQL Editor and run:

```text
database/schema.sql
```

## Current endpoints

- `GET /health`
- `GET /api/jobs`
- `POST /api/jobs`

## Next build step

- Connect Supabase
- Add real job persistence
- Add user profile and master CV
- Add visa-sponsorship detection
